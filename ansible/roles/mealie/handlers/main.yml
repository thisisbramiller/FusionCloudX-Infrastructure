---
# ==============================================================================
# Mealie Role - Handlers
# ==============================================================================

- name: restart mealie
  community.docker.docker_compose_v2:
    project_src: "{{ mealie_compose_dir }}"
    state: present
    recreate: always
  become: yes

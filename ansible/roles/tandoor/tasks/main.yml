---
# ==============================================================================
# Tandoor Role - Main Tasks
# ==============================================================================
# Installs Docker and deploys Tandoor Recipes via Docker Compose
# ==============================================================================

- name: Install Docker prerequisites
  apt:
    name:
      - ca-certificates
      - curl
      - gnupg
    state: present
    update_cache: yes
  tags: ['tandoor', 'docker']

- name: Add Docker GPG key
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/ubuntu/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: '0644'
  tags: ['tandoor', 'docker']

- name: Add Docker repository
  ansible.builtin.apt_repository:
    repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu {{ ansible_distribution_release }} stable"
    state: present
    filename: docker
  tags: ['tandoor', 'docker']

- name: Install Docker CE and Compose plugin
  apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-compose-plugin
    state: present
    update_cache: yes
  tags: ['tandoor', 'docker']

- name: Ensure Docker service is started and enabled
  systemd:
    name: docker
    state: started
    enabled: yes
  tags: ['tandoor', 'docker']

- name: Fetch Tandoor database credentials from 1Password
  onepassword.connect.field_info:
    token: "{{ lookup('env', 'OP_CONNECT_TOKEN') }}"
    item: "{{ tandoor_onepassword_db_item }}"
    field: "password"
    vault: "{{ lookup('env', 'TF_VAR_onepassword_vault_id') }}"
  environment:
    OP_CONNECT_HOST: "{{ lookup('env', 'OP_CONNECT_HOST') }}"
  delegate_to: localhost
  become: no
  no_log: true
  register: tandoor_db_password_result
  tags: ['tandoor', 'secrets']

- name: Set database credential variables
  set_fact:
    tandoor_db_user: "tandoor"
    tandoor_db_password: "{{ tandoor_db_password_result.field.value }}"
  no_log: true
  tags: ['tandoor', 'secrets']

- name: Generate Tandoor SECRET_KEY
  set_fact:
    tandoor_secret_key: "{{ lookup('ansible.builtin.password', '/dev/null chars=ascii_letters,digits length=50') }}"
  no_log: true
  tags: ['tandoor', 'secrets']

- name: Check if secret key file already exists
  stat:
    path: "{{ tandoor_compose_dir }}/.secret_key"
  register: tandoor_secret_key_file
  tags: ['tandoor', 'secrets']

- name: Read existing secret key
  slurp:
    src: "{{ tandoor_compose_dir }}/.secret_key"
  register: tandoor_existing_secret_key
  when: tandoor_secret_key_file.stat.exists
  no_log: true
  tags: ['tandoor', 'secrets']

- name: Use existing secret key if available
  set_fact:
    tandoor_secret_key: "{{ tandoor_existing_secret_key.content | b64decode }}"
  when: tandoor_secret_key_file.stat.exists
  no_log: true
  tags: ['tandoor', 'secrets']

- name: Create Tandoor compose directory
  file:
    path: "{{ tandoor_compose_dir }}"
    state: directory
    owner: root
    group: root
    mode: '0750'
  tags: ['tandoor', 'deploy']

- name: Persist secret key to file
  copy:
    content: "{{ tandoor_secret_key }}"
    dest: "{{ tandoor_compose_dir }}/.secret_key"
    owner: root
    group: root
    mode: '0600'
  no_log: true
  when: not tandoor_secret_key_file.stat.exists
  tags: ['tandoor', 'deploy']

- name: Template Docker Compose file
  template:
    src: docker-compose.yml.j2
    dest: "{{ tandoor_compose_dir }}/docker-compose.yml"
    owner: root
    group: root
    mode: '0640'
  notify: restart tandoor
  tags: ['tandoor', 'deploy']

- name: Template environment file
  template:
    src: .env.j2
    dest: "{{ tandoor_compose_dir }}/.env"
    owner: root
    group: root
    mode: '0600'
  no_log: true
  notify: restart tandoor
  tags: ['tandoor', 'deploy']

- name: Template nginx configuration
  template:
    src: nginx.conf.j2
    dest: "{{ tandoor_compose_dir }}/nginx.conf"
    owner: root
    group: root
    mode: '0644'
  notify: restart tandoor
  tags: ['tandoor', 'deploy']

- name: Start Tandoor containers
  community.docker.docker_compose_v2:
    project_src: "{{ tandoor_compose_dir }}"
    state: present
    pull: always
  tags: ['tandoor', 'deploy']

- name: Wait for Tandoor to be ready
  uri:
    url: "https://localhost:{{ tandoor_https_port }}"
    validate_certs: no
    status_code: [200, 302]
  register: tandoor_health
  retries: 30
  delay: 5
  until: tandoor_health is succeeded
  tags: ['tandoor', 'verify']

- name: Display Tandoor deployment status
  debug:
    msg:
      - "========================================"
      - "Tandoor Recipes deployment complete!"
      - "========================================"
      - "URL: https://{{ ansible_host }}:{{ tandoor_https_port }}"
      - "Database: {{ tandoor_db_name }} on {{ tandoor_db_host }}"
      - "========================================"
  tags: ['tandoor']
